function _test() {
  SpreadsheetApp.getActiveSheet().getRange("A2").expandGroups()
}

function _manualTrigger() {
  MBInventorySystem.manualTrigger("F108")
}

function _onOpen() {
  MBInventorySystem.registerCurrentSheets(PropertiesService);
}

function _removeConflictedSheets() {
  MBInventorySystem.removeConflictedSheets();
}

function _installedOnChange(e) {
  MBInventorySystem.deleteUnregisteredSheets(e, PropertiesService, LockService);
}

function _installedOnEditTrigger (e) {
  MBInventorySystem.installedOnEditTriggerInv(e, () => PropertiesService.getScriptProperties(), () => LockService.getScriptLock(), 'PRD')
}
